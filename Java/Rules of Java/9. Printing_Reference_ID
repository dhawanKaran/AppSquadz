Whenever we try to print the reference id of an object JVM prints a string rather than reference id of an object which is a combination of three things.
   a) class name
   b) @ (at) symbol
   c) hexa decimal representation of the hashcode of a object.